services:
  - type: web
    name: stock-picker
    runtime: python
    plan: starter
    autoDeploy: false
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    healthCheckPath: /
